<UserControl x:Class="RssReader.View.UserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RssReader"
             xmlns:views="clr-namespace:RssReader.View"
             xmlns:viewModels="clr-namespace:RssReader.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ListView Grid.Row="0" Margin="3">
                <ListView.InputBindings>
                    <KeyBinding Key="Space" Command="{Binding Path=SwitchSelectedFeedCommand}"/>
                </ListView.InputBindings>
                
                <ListView.SelectedItem>
                    <Binding Path="SelectedFeed" Mode="TwoWay"></Binding>
                </ListView.SelectedItem>
                
                <ListView.ItemsSource>
                    <Binding Path="FeedsList"/>
                </ListView.ItemsSource>
                
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <views:FeedView />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                
                <Button Grid.Column="0">
                    <Button.Command>
                        <Binding Path="SelectAllFeedsCommand"></Binding>
                    </Button.Command>
                    
                    <TextBlock>All</TextBlock>
                </Button>
                
                <Button Grid.Column="1">
                    <Button.Command>
                        <Binding Path="UnselectAllFeedsCommand"></Binding>
                    </Button.Command>
                    
                    <TextBlock>None</TextBlock>
                </Button>
            </Grid>
            
            <Button Grid.Row="2" Margin="5">Update</Button>
        </Grid>

        <ListView Grid.Column="1" Margin="3">
            <ListView.ItemTemplate>
                <DataTemplate>

                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
    </Grid>
</UserControl>
